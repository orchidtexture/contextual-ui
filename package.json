{
  "name": "contextual-ui-monorepo",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "pnpm": {
    "allowBuilds": {
      "esbuild": true
    }
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "typescript": "^7.0.2",
    "tsup": "^8.5.1"
  }
}
