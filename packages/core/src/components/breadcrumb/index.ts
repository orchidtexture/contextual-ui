import { Root, List, Item, Link, Page, Separator } from './Breadcrumb';

export const Breadcrumb = {
  Root,
  List,
  Item,
  Link,
  Page,
  Separator,
};

export type { BreadcrumbItem, BreadcrumbData } from './breadcrumb.schema';
export * from './breadcrumb.types';
